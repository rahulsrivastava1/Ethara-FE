# API URL baked into the frontend at build time.
# Leave empty to use /api (proxied by nginx to API_UPSTREAM).
VITE_API_BASE_URL=

# Backend URL for nginx to proxy /api requests (runtime, docker only).
API_UPSTREAM=http://host.docker.internal:8000

# Host port to expose the frontend on.
PORT=3000
